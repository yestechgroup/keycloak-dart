name: keycloak_dart
description: A Dart adapter for Keycloak authentication, providing the same functionality as keycloak-js for Flutter applications.
version: 0.1.0
repository: https://github.com/yestech/keycloak-dart

environment:
  sdk: ^3.0.0

dependencies:
  crypto: ^3.0.0
  http: ^1.2.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
  mockito: ^5.4.0
  build_runner: ^2.4.0
